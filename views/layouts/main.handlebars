<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blue 42</title>
    <link rel="stylesheet" href='/stylesheets/style.css'
</head>
<body>
    <header>
        <div class='container'>
        <img src='/assets/lucas-andrade-M8uBaLEyGV4-unsplash.jpg' width="1600" height="1200" alt="Football field at night">
        <nav class="navbar">
            {{!-- {{#if loggedIn}} --}}
            {{!-- Will replace with the href once it's created --}}
            <button id='myTeam' class='navbtn'>My Team</button>
            <button id='chooseTeams' class='navbtn'>Team Selection</button>
            <button id='viewMatchups' class='navbtn'>View Matchups</button>
            
            <button id='logout' class='navbtn'>Logout</button>
            {{!-- {{else}} --}}
            {{!-- Will replace with the href once it's created --}}
            <button id='login' class='navbtn'>Login</button>
            {{!-- {{/if}} --}}
        </nav>
        <h1 class="header-h1">Blue 42</h1>
        </div>
    </header>
    <main>

    </main>
    <footer>

    </footer>
</body>
</html>